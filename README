To setup:

First start up mongodb on your machine.

Then run:

$ npm install

Then run:

$ npm start


If this is your first time running it, you'll have to 
get an api token for use of the Google calendar API. 
Go to the following url and follow the instructions to 
get a client_secret.json file. Put this in the main 
fitness_buddy directory.

https://developers.google.com/google-apps/calendar/quickstart/nodejs